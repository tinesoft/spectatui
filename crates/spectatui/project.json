{
  "name": "spectatui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "./crates/spectatui/src",
  "targets": {
    "build": {
      "cache": true,
      "executor": "@monodon/rust:build",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/spectatui"
      },
      "configurations": {
        "production": { "release": true }
      }
    },
    "test": {
      "cache": true,
      "executor": "@monodon/rust:test",
      "options": {
        "target-dir": "dist/target/spectatui"
      },
      "configurations": {
        "production": { "release": true }
      }
    },
    "lint": {
      "cache": true,
      "executor": "@monodon/rust:lint",
      "options": {
        "target-dir": "dist/target/spectatui",
        "args": ["-D", "warnings"]
      }
    },
    "check": {
      "cache": true,
      "executor": "@monodon/rust:check",
      "options": {
        "target-dir": "dist/target/spectatui"
      }
    },
    "fmt": {
      "cache": true,
      "executor": "nx:run-commands",
      "options": {
        "command": "cargo fmt -p spectatui -- --check"
      }
    },
    "run": {
      "executor": "@monodon/rust:run",
      "outputs": ["{options.target-dir}"],
      "options": {
        "target-dir": "dist/target/spectatui"
      },
      "configurations": {
        "production": { "release": true }
      }
    }
  },
  "tags": []
}
